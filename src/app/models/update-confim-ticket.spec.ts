import { UpdateConfimTicket } from './update-confim-ticket';

describe('UpdateConfimTicket', () => {
  it('should create an instance', () => {
    expect(new UpdateConfimTicket()).toBeTruthy();
  });
});
