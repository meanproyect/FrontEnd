import { ClientDatosModels } from './client-datos-models';

describe('ClientDatosModels', () => {
  it('should create an instance', () => {
    expect(new ClientDatosModels()).toBeTruthy();
  });
});
