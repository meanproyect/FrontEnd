import { AdministratorDatosModels } from './administrator-datos-models';

describe('AdministratorDatosModels', () => {
  it('should create an instance', () => {
    expect(new AdministratorDatosModels()).toBeTruthy();
  });
});
